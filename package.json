{
  "name": "openrouter-review",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub Action that reviews PRs via OpenRouter",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --license licenses.txt",
    "package": "npm run build",
    "test": "node --test"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  }
}
